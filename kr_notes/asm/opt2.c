#include <stdio.h>
/* the compiler concatenates the strings. See this in the assembly */

int main()
{
    int c = 75;
    printf("the number is %d", c);
    
    // char str[] = "hi" "ther";
    // printf("%s",str);

    return 0;
}